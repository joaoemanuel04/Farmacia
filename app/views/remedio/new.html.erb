<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastrar Remédio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Cadastrar Novo Remédio</h1>

        <%= form_with model: @remedio, local: true, class: 'mt-4' do |form| %>
            <div class="form-group">
                <%= form.label :Remediocol, 'Nome do Remédio' %>
                <%= form.text_field :Remediocol, class: 'form-control', placeholder: 'Nome do remédio' %>
            </div>

            <div class="form-group">
                <%= form.label :fabricante, 'Fabricante' %>
                <%= form.text_field :fabricante, class: 'form-control', placeholder: 'Fabricante do remédio' %>
            </div>

            <div class="form-group">
                <%= form.label :tipo, 'Tipo de Remédio' %>
                <%= form.text_field :tipo, class: 'form-control', placeholder: 'Tipo do remédio' %>
            </div>

            <div class="form-group">
                <%= form.label :preco, 'Preço (R$)' %>
                <%= form.number_field :preco, step: 0.01, class: 'form-control', placeholder: 'Preço do remédio' %>
            </div>

            <div class="form-group mt-4">
                <%= form.submit 'Cadastrar Remédio', class: 'btn btn-success' %>
            </div>
        <% end %>

        <div class="mt-3">
            <%= link_to 'Voltar para Lista de Remédios', remedios_path, class: 'btn btn-primary' %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>