<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BioFarma - Cadastro de Atendente</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f9ff;
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #0078d4;
            color: white;
            padding: 20px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 10%;
        }

        header .logo {
            font-family: 'Montserrat', sans-serif;
            font-size: 4em;
            font-weight: 700;
            color: white;
        }

        header nav a {
            color: white;
            text-decoration: none;
            margin: 0 20px;
            font-weight: 600;
            font-size: 1.2em;
        }

        header nav a:hover {
            text-decoration: underline;
        }

        .container {
            background: #fff;
            color: #333;
            border-radius: 8px;
            padding: 20px;
            margin-top: 30px;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        }

        h1, h2 {
            color: #0078d4;
            font-weight: bold;
        }

        .btn-primary {
            background-color: #0078d4;
            border: none;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .btn-success {
            background-color: #28a745;
            border: none;
        }

        .btn-success:hover {
            background-color: #218838;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-control {
            border-radius: 5px;
        }

        #error_explanation {
            background-color: #f8d7da;
            color: #721c24;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">BioFarma</div>
        <nav>
            <a href="#home">Início</a>
            <a href="#about">Sobre</a>
            <a href="#contact">Contato</a>
        </nav>
    </header>

    <div class="container">
        <h1 class="text-center">Bem-vindo, <%= current_user.email %>!</h1>
        <h2 class="text-center">Cadastro de Atendente</h2>

        <%= form_with(model: @atendente, url: atendentes_path, local: true) do |form| %>
            <% if @atendente.errors.any? %>
                <div id="error_explanation">
                    <h2><%= pluralize(@atendente.errors.count, "erro") %> proibiu este atendente de ser salvo:</h2>
                    <ul>
                        <% @atendente.errors.full_messages.each do |message| %>
                            <li><%= message %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>

            <div class="mb-3">
                <%= form.label :Nome, class: 'form-label' %>
                <%= form.text_field :Nome, class: 'form-control', placeholder: 'Nome do atendente' %>
            </div>

            <div class="mb-3">
                <%= form.label :CPF, class: 'form-label' %>
                <%= form.text_field :CPF, class: 'form-control', placeholder: 'CPF do atendente' %>
            </div>

            <div class="mb-3">
                <%= form.label :dt_nasce, class: 'form-label' %>
                <%= form.date_select :dt_nasce, start_year: 1900, end_year: Time.now.year, class: 'form-control' %>
            </div>

            <div class="text-center">
                <%= form.submit "Cadastrar Atendente", class: 'btn btn-success' %>
            </div>
        <% end %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
